#define Complex_h


struct ch //объ€вл€ем структуру
{
	double x; 
	double y; 
}; 
bool t(double a, double b);

void p(ch nom1, ch nom2); //сл
void m(ch nom1, ch nom2); //вычит
void y(ch nom1, ch nom2); //умнож
void d(ch nom1, ch nom2); //дел

